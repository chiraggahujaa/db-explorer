{
  "name": "db-mcp",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/server-bun-sse.ts",
    "dev": "bun run --watch src/server-bun-sse.ts",
    "build": "bun build src/server-bun-sse.ts --outdir dist --target bun",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.18.0",
    "mysql2": "^3.14.5",
    "mongodb": "^6.9.0",
    "zod": "^4.1.8"
  }
}
